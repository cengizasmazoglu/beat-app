specVersion: "v1beta"
serviceId: "beat-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "beat-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
